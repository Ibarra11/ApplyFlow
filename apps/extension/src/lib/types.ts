/**
 * Resume domain types and helpers are defined once in the shared
 * `@applyflow/schema` package (as Zod schemas) and re-exported here so the
 * rest of the extension can keep importing from `@/lib/types`.
 */
export * from "@applyflow/schema";
